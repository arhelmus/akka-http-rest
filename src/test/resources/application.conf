akka {
  loglevel = WARNING
}

database = {
  url = "jdbc:postgresql://postgres:test@localhost/akka-http-rest-test"
  url = ${?DATABASE_URL}
  driver = org.postgresql.Driver
  connectionPool = disabled
  keepAliveConnection = true
}
akka {
  loglevel = WARNING
}

database = {
  url = "jdbc:postgresql://localhost:25535/database-name?user=user&password=password"
  url = ${?PSQL_TEST_URL}
  user = "user"
  user = ${?PSQL_TEST_USER}
  password = "password"
  password = ${?PSQL_TEST_PASSWORD}
  driver = org.postgresql.Driver
  connectionPool = disabled
  keepAliveConnection = true
}